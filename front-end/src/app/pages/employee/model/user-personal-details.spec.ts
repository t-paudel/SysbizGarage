import { UserPersonalDetails } from './user-personal-details';

describe('UserPersonalDetails', () => {
  it('should create an instance', () => {
    expect(new UserPersonalDetails()).toBeTruthy();
  });
});
