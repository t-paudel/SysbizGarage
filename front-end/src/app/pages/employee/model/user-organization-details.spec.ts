import { UserOrganizationDetails } from './user-organization-details';

describe('UserOrganizationDetails', () => {
  it('should create an instance', () => {
    expect(new UserOrganizationDetails()).toBeTruthy();
  });
});
